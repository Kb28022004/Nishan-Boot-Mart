import React from "react";
import HeroSection from "./components/HeroSection";

const About = () => {
  const myData = {
    name: "Nishan Ecommerce",
  };

  return <HeroSection data={myData} />;
};

export default About;